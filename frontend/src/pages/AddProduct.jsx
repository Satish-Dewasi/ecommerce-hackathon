import ProductForm from "@/components/ProductForm";

// This page is used to add a new product and reuses the ProductForm component in "add" mode.
const AddProduct = () => <ProductForm mode="add" />;

export default AddProduct;
